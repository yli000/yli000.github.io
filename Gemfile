source "https://rubygems.org"

# 与 GitHub Pages 线上环境一致（Jekyll 3.10）。
# 不要改成 gem "jekyll", "~> 4.x"：会和 jekyll-remote-theme 冲突，
# 还会拉入 openssl 原生扩展，在 Windows 上编译不过。
gem "github-pages", group: :jekyll_plugins

# Ruby 3 起标准库移除了 webrick，jekyll serve 需要它。
gem "webrick"
